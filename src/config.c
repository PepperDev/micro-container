#include "config.h"
#include <stdio.h>

bool config_parse(config_t * config, int argc, char *argv[])
{
	/*
    int opt;
    while ((opt = getopt_long(argc, argv, "nt:", ..., ...)) != -1) {
        switch (opt) {
        case 'a':
            break;
        default:
            fprintf(stderr, "Usage: %s ...\n", argv[0]);
            exit(EXIT_FAILURE);
        }
    }
    */
    return true;
}
